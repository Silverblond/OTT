spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

ocr:
  save-dir: uploads

translate:
  default-lang: ko

  naver:
    papago:
      client-id: YOUR_CLIENT_ID
      client-secret: YOUR_CLIENT_SECRET
